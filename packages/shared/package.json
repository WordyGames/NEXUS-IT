{
  "name": "@nexus-it/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json.build",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "firebase": "^10.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
