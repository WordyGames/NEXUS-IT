{
  "name": "@nexus-it/shared",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "react-native": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json.build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "npm run typecheck",
    "test": "vitest run",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "firebase": "^10.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3",
    "vitest": "^2.1.8"
  }
}
